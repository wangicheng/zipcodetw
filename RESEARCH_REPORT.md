# ZipCodeTw 全二進制零展開郵遞區號解析引擎研究報告

## 1. 研究動機與挑戰

台灣 3+3 碼郵遞區號系統涵蓋全台 **79,876 筆** 門牌對照規則。其結構包含單雙號、連號、附號（如 `1之23號`）、樓層與地下室（如 `2樓以上`、`地下1樓至1樓`）等多維度條件。

傳統解析方案存在三大瓶頸：
1. **傳輸與儲存體積龐大**：未經二進制壓縮的門牌規則結構達 12 MB 以上，無法直接發布於前端或邊緣節點。
2. **記憶體與 GC 負擔**：傳統引擎在啟動時將字串與規則物件完整展開至記憶體，產生逾 44,000 個字串物件與 79,000 個規則物件，導致 V8 堆記憶體與進程 RSS 暴增 164 MB 以上。
3. **冷啟動耗時長**：傳統 JSON 解析與搜尋樹建立需耗時 200 至 250 毫秒，難以滿足現代 Serverless 邊緣架構需求。

本專案的核心目標在於：設計全二進制零展開（Dual Binary Zero-Expansion）架構，達成 0ms 級冷啟動、微秒級查詢速度與小於 10MB 的記憶體佔用。

---

## 2. 二進位資產架構與核心技術

本系統將 79,876 筆資料編譯為兩份結構化的自訂二進位資產：`address_prefixes.bin` 與 `zipcode_rules.bin`。

```
[ 中華郵政 rall1.dbf (12.2 MB) ]
         │
         ▼ (純 TypeScript DBFReader 記憶體解析)
[ 記憶體 RawAddress 陣列 ]
         │
         ▼ (二進制編譯器)
┌─────────────────────────────────────┬─────────────────────────────────────┐
│    address_prefixes.bin (1.26 MB)   │      zipcode_rules.bin (1.33 MB)    │
│  (區塊化 Front Coding + 預建倒排索引)│    (定長 Index Table + Bitmask 標頭) │
└─────────────────────────────────────┴─────────────────────────────────────┘
         │                                     │
         └──────────────────┬──────────────────┘
                            ▼
           [ BinaryPrefixSearchEngine ] 
           [     BinaryRuleStore      ]
           (靜態 Uint8Array 零複製比對)
```

### 2.1 門牌前綴區塊化與預建倒排索引 (`address_prefixes.bin`)

針對 44,658 條不重複門牌前綴字串，結合區塊化前綴編碼（Block-Based Binary Front Coding）與預建倒排索引串流：

- **32 位元組標頭 (`ZPF1`)**：記錄字元映射表、倒排索引串流、區塊索引與文字流偏移。
- **預建倒排索引串流**：編譯階段即為 1,807 個 Unicode 字元建立靜態 `Uint16Array` 倒排索引串流。查詢時直接透過 TypedArray 指標進行雙指標 $O(N+M)$ 交集，無須於執行期動態解碼字串或建置 Map 索引。
- **區塊化惰性解碼**：將 44,658 條字串按 64 條劃分為 698 個獨立區塊。查詢時僅針對倒排索引過濾出的候選字串 ID（每次查詢約 1 至 3 條）實施單一區塊 UTF-8 解碼，剩餘 99.9% 的字串保持二進位狀態，達成零展開（Zero-Expansion）。

### 2.2 門牌規則二進制與零複製比對 (`zipcode_rules.bin`)

79,876 筆門牌規則編譯為 1.33 MB 二進位檔，較原始 JSON 結構 6.11 MB 減少 77.5%：

- **32 位元組標頭 (`ZPR1`)**：定義郵遞區號字典、大宗戶名稱池與規則索引表偏移。
- **10 位元組定長索引表**：每筆規則固定佔用 10 位元組（`part1Index: uint16`, `zipcodeId: uint16`, `bulkNameId: uint16`, `ruleStreamOffset: uint32`），可經由 $O(1)$ 指標移位讀取。
- **2 位元組控制標頭**：
  - `標頭位元組 1`：使用位元遮罩標示數值、最小值、最大值、奇偶性 (`parity`: 0=無, 1=單, 2=雙, 3=連) 與子號模式 (`subMode`: 0=無, 1=all, 2=sub_all)。
  - `標頭位元組 2`：以 4-bit Enum 編碼門牌單位 (`unit`: 1=號, 2=巷, 3=樓, 4=弄, 5=附號) 與結束單位。
- **零複製位元比對**：`BinaryRuleStore` 接收查詢門牌數值，直接於 Buffer 偏移位進行位元遮罩運算，比對過程零物件配置（Zero Allocations）。

### 2.3 純 TypeScript 端到端資料管線

淘汰 Python 腳本與硬碟中間檔落檔過程：
- 實作 [fetch_official_dbf.ts](file:///c:/dev/zipcodetw/tools/data-crawler/fetch_official_dbf.ts)，使用 Node/Bun 原生 `TextDecoder('big5')` 於記憶體中解析中華郵政 `rall1.dbf`。
- 直接在記憶體中串流輸出二進位資產，避免中間檔的硬碟 I/O，建置速度提升 30%。

---

## 3. 實驗數據與效能報告

測試環境：Windows 11 x64, Bun v1.3.5, Intel Core i7 / Node.js 執行階段，針對 79,876 筆門牌對照資料實測。

### 3.1 儲存體積與網路傳輸壓縮率

| 資產層級 | 檔案類型 / 傳輸協定 | 檔案體積 | 說明與縮減率 |
| :--- | :--- | :---: | :--- |
| **官方 DBF 資料庫** | `rall1.dbf`（原始檔） | 12.20 MB | 官方來源對照檔 |
| **二進位前綴檔 (Part 1)** | `address_prefixes.bin` | 1.26 MB | 含 1,807 字元預建倒排索引 |
| **二進位規則檔 (Part 2)** | `zipcode_rules.bin` | 1.33 MB | 較傳統結構減少 77.5% |
| **二進位資產總體積** | **Disk Asset Total** | **2.58 MB** | 較官方原始檔壓縮 **-78.8%** |
| **HTTP 傳輸體積 (Gzip)** | Gzip 傳輸總量 | 1.43 MB | 較二進位資產再減少 -44.5% |
| **HTTP 傳輸體積 (Brotli)**| Brotli 傳輸總量 | **791.96 KB** (0.77 MB) | 較二進位資產再減少 **-69.3%** |

### 3.2 冷啟動耗時與記憶體佔用

經由 `measure_memory.ts` 進行進程記憶體快照比對：

| 效能與資源指標 | 傳統 JSON 展開引擎 | 全二進制零展開引擎 | 改善幅度 |
| :--- | :---: | :---: | :---: |
| **引擎載入耗時 (Load Time)** | 246.45 ms | **12.11 ms** | **-95.10%** |
| **V8 Heap 堆記憶體淨增長** | 35.74 MB | **0.00 MB** | **-100.00%** |
| **進程總記憶體淨增長 (RSS Delta)** | 164.86 MB | **9.27 MB** | **-94.38%** |

### 3.3 查詢效能

在 10,000 次隨機實際門牌查詢測試結果：

| 查詢效能指標 | 傳統 JSON 引擎 | 全二進制零展開引擎 | 備註 |
| :--- | :---: | :---: | :--- |
| **10,000 次查詢總耗時** | 666.43 ms | 717.27 ms | 效能等級相同 |
| **單次查詢平均耗時** | 0.0666 ms | **0.0717 ms** (71.73 微秒) | 單執行緒每秒處理 14,000+ 次查詢 |

---

## 4. 結論

本專案解決了傳統郵遞區號解析引擎傳輸龐大與記憶體開銷高昂的問題：
1. 建立 Block-Based Front Coding、預建倒排索引與 Bitmask 規則儲存區之全二進位零展開架構。
2. 全台門牌資料庫網路傳輸體積降至 791.96 KB (Brotli)。
3. 達成 12ms 快速冷啟動、0 MB V8 堆記憶體額外佔用與 71 微秒單次查詢耗時。

全套程式碼與資料管線已通過 50/50 筆單元測試與 Biome 檢查。
