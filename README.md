# zipcodetw

最快最方便的離線台灣 3+3 郵遞區號查詢系統。

- 即時反應
- 模糊查詢
- 條件匹配
- 離線運作

## 使用方式

- 安裝相依套件（建議使用 `bun`）：

```
bun install
```

 也可使用 `npm install` 或 `pnpm install`。

- 產生地址資料檔（會建立本專案需要的資料來源）：

```
bun run build:data
```
